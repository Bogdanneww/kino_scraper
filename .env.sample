# Base URL for scraping
BASE_URL=YOUR_BASE_URL_HERE

# HTTP client timeout (seconds)
HTTP_TIMEOUT=YOUR_TIMEOUT_IN_SECONDS

# Playwright
# True  -> headless (Docker / CI)
# False -> visible browser (local debug)
PLAYWRIGHT_HEADLESS=TRUE_OR_FALSE
